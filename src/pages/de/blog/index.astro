---
import { getCollection } from 'astro:content';
import BlogListPage from '../../../components/BlogListPage.astro';

const posts = await getCollection('blog', ({ id }) => id.startsWith('de/'));
---
<BlogListPage lang="de" posts={posts} />
